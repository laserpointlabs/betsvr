## betsvr environment (example)
#
# Copy to `.env` (or export vars in your shell) and fill values, then run:
#   docker compose up -d --build
#
# IMPORTANT: Do not commit `.env` (it contains secrets).

# Used by bet_api -> betting_monitor (The Odds API). REQUIRED for prop bets and line monitoring.
ODDS_API_KEY=replace_me

# Used by bet_api to validate tokens against lmsvr / lmapi.
LM_API_BASE_URL=https://lmapi.laserpointlabs.com

# Optional tuning
LOG_LEVEL=INFO
ALERT_TTL_MINUTES=60
PROP_CHECK_INTERVAL=15

# Frontend default model (optional)
BET_DEFAULT_MODEL=granite4:3b


